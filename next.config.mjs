/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "cdn.sanity.io",
            "https://www.sanity.io",
        ],
    },
};

export default nextConfig;